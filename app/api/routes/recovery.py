"""
Recovery API routes — placeholder.

FastAPI router for recovery endpoints.
Not yet implemented — endpoints will be added in the next development phase.
"""

from __future__ import annotations

from fastapi import APIRouter

router = APIRouter(prefix="/recovery", tags=["recovery"])

# Routes will be added here, e.g.:
# @router.post("/decide")
# async def decide_recovery(case: ...) -> ...:
#     ...
